<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:lang="http://www.springframework.org/schema/lang"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd
           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-2.0.xsd
           http://www.springframework.org/schema/lang http://www.springframework.org/schema/lang/spring-lang-2.0.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.0.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd"
	default-autowire="byName">

	<import resource="classpath:spring/base-instance.xml" />

	<bean id="instance" class="com.alibaba.otter.canal.instance.spring.CanalInstanceWithSpring">
		<property name="destination" value="${canal_instance_destination}" />
		<property name="eventParser">
			<ref local="eventParser" />
		</property>
		<property name="eventSink">
			<ref local="eventSink" />
		</property>
		<property name="eventStore">
			<ref local="eventStore" />
		</property>
		<property name="metaManager">
			<ref local="metaManager" />
		</property>
		<property name="alarmHandler">
			<ref local="alarmHandler" />
		</property>
        <property name="mqConfig">
            <ref local="mqConfig" />
        </property>
	</bean>

	<!-- 报警处理类 -->
	<bean id="alarmHandler" class="com.alibaba.otter.canal.common.alarm.LogAlarmHandler" />

	<bean id="metaManager" class="com.alibaba.otter.canal.meta.FileMixedMetaManager">
		<property name="dataDir" value="${canal_file_data_dir:../conf}" />
		<property name="period" value="${canal_file_flush_period:1000}" />
	</bean>

	<bean id="eventStore" class="com.alibaba.otter.canal.store.memory.MemoryEventStoreWithBuffer">
		<property name="bufferSize" value="${canal_instance_memory_buffer_size:16384}" />
		<property name="bufferMemUnit" value="${canal_instance_memory_buffer_memunit:1024}" />
		<property name="batchMode" value="${canal_instance_memory_batch_mode:MEMSIZE}" />
		<property name="ddlIsolation" value="${canal_instance_get_ddl_isolation:false}" />
		<property name="raw" value="${canal_instance_memory_rawEntry:true}" />
	</bean>

	<bean id="eventSink" class="com.alibaba.otter.canal.sink.entry.EntryEventSink">
		<property name="eventStore" ref="eventStore" />
		<property name="filterTransactionEntry" value="${canal_instance_filter_transaction_entry:false}"/>
	</bean>

	<bean id="eventParser" parent="baseEventParser">
		<property name="destination" value="${canal_instance_destination}" />
		<property name="slaveId" value="${canal_instance_mysql_slaveId:0}" />
		<!-- 心跳配置 -->
		<property name="detectingEnable" value="${canal_instance_detecting_enable:false}" />
		<property name="detectingSQL" value="${canal_instance_detecting_sql}" />
		<property name="detectingIntervalInSeconds" value="${canal_instance_detecting_interval_time:5}" />
		<property name="haController">
			<bean class="com.alibaba.otter.canal.parse.ha.HeartBeatHAController">
				<property name="detectingRetryTimes" value="${canal_instance_detecting_retry_threshold:3}" />
				<property name="switchEnable" value="${canal_instance_detecting_heartbeatHaEnable:false}" />
			</bean>
		</property>

		<property name="alarmHandler" ref="alarmHandler" />

		<!-- 解析过滤处理 -->
		<property name="eventFilter">
			<bean class="com.alibaba.otter.canal.filter.aviater.AviaterRegexFilter" >
				<constructor-arg index="0" value="${canal_instance_filter_regex:.*\..*}" />
			</bean>
		</property>

		<property name="eventBlackFilter">
			<bean class="com.alibaba.otter.canal.filter.aviater.AviaterRegexFilter" >
				<constructor-arg index="0" value="${canal_instance_filter_black_regex:}" />
				<constructor-arg index="1" value="false" />
			</bean>
		</property>
		
		<property name="fieldFilter" value="${canal_instance_filter_field}" />
		<property name="fieldBlackFilter" value="${canal_instance_filter_black_field}" />
		
		<!-- 最大事务解析大小，超过该大小后事务将被切分为多个事务投递 -->
		<property name="transactionSize" value="${canal_instance_transaction_size:1024}" />

		<!-- 网络链接参数 -->
		<property name="receiveBufferSize" value="${canal_instance_network_receiveBufferSize:16384}" />
		<property name="sendBufferSize" value="${canal_instance_network_sendBufferSize:16384}" />
		<property name="defaultConnectionTimeoutInSeconds" value="${canal_instance_network_soTimeout:30}" />

		<!-- 解析编码 -->
		<!-- property name="connectionCharsetNumber" value="${canal.instance.connectionCharsetNumber:33}" /-->
		<property name="connectionCharset" value="${canal_instance_connectionCharset:UTF-8}" />

		<!-- 解析位点记录 -->
		<property name="logPositionManager">
			<bean class="com.alibaba.otter.canal.parse.index.FailbackLogPositionManager">
				<constructor-arg>
					<bean class="com.alibaba.otter.canal.parse.index.MemoryLogPositionManager" />
				</constructor-arg>
				<constructor-arg>
					<bean class="com.alibaba.otter.canal.parse.index.MetaLogPositionManager">
						<constructor-arg ref="metaManager"/>
					</bean>
				</constructor-arg>
			</bean>
		</property>

		<!-- failover切换时回退的时间 -->
		<property name="fallbackIntervalInSeconds" value="${canal_instance_fallbackIntervalInSeconds:60}" />

		<!-- 解析数据库信息 -->
		<property name="masterInfo">
			<bean class="com.alibaba.otter.canal.parse.support.AuthenticationInfo" init-method="initPwd">
				<property name="address" value="${canal_instance_master_address}" />
				<property name="username" value="${canal_instance_dbUsername:retl}" />
				<property name="password" value="${canal_instance_dbPassword:retl}" />
				<property name="pwdPublicKey" value="${canal_instance_pwdPublicKey:retl}" />
				<property name="enableDruid" value="${canal_instance_enableDruid:false}" />
				<property name="defaultDatabaseName" value="${canal_instance_defaultDatabaseName:}" />
			</bean>
		</property>
		<property name="standbyInfo">
			<bean class="com.alibaba.otter.canal.parse.support.AuthenticationInfo" init-method="initPwd">
				<property name="address" value="${canal_instance_standby_address}" />
				<property name="username" value="${canal_instance_dbUsername:retl}" />
				<property name="password" value="${canal_instance_dbPassword:retl}" />
				<property name="pwdPublicKey" value="${canal_instance_pwdPublicKey:retl}" />
				<property name="enableDruid" value="${canal_instance_enableDruid:false}" />
				<property name="defaultDatabaseName" value="${canal_instance_defaultDatabaseName:}" />
			</bean>
		</property>

		<!-- 解析起始位点 -->
		<property name="masterPosition">
			<bean class="com.alibaba.otter.canal.protocol.position.EntryPosition">
				<property name="journalName" value="${canal_instance_master_journal_name}" />
				<property name="position" value="${canal_instance_master_position}" />
				<property name="timestamp" value="${canal_instance_master_timestamp}" />
				<property name="gtid" value="${canal_instance_master_gtid}" />
			</bean>
		</property>
		<property name="standbyPosition">
			<bean class="com.alibaba.otter.canal.protocol.position.EntryPosition">
				<property name="journalName" value="${canal_instance_standby_journal_name}" />
				<property name="position" value="${canal_instance_standby_position}" />
				<property name="timestamp" value="${canal_instance_standby_timestamp}" />
				<property name="gtid" value="${canal_instance_standby_gtid}" />
			</bean>
		</property>
		<property name="filterQueryDml" value="${canal_instance_filter_query_dml:false}" />
		<property name="filterQueryDcl" value="${canal_instance_filter_query_dcl:false}" />
		<property name="filterQueryDdl" value="${canal_instance_filter_query_ddl:false}" />
		<property name="useDruidDdlFilter" value="${canal_instance_filter_druid_ddl:true}" />
		<property name="filterRows" value="${canal_instance_filter_rows:false}" />
		<property name="filterTableError" value="${canal_instance_filter_table_error:false}" />
		<property name="supportBinlogFormats" value="${canal_instance_binlog_format}" />
		<property name="supportBinlogImages" value="${canal_instance_binlog_image}" />

		<!--表结构相关-->
		<property name="enableTsdb" value="${canal_instance_tsdb_enable:true}"/>
		<property name="tsdbSpringXml" value="${canal_instance_tsdb_spring_xml:}"/>
		<property name="tsdbSnapshotInterval" value="${canal_instance_tsdb_snapshot_interval:24}" />
		<property name="tsdbSnapshotExpire" value="${canal_instance_tsdb_snapshot_expire:360}" />

		<!--是否启用GTID模式-->
		<property name="isGTIDMode" value="${canal_instance_gtidon:false}"/>

		<!-- parallel parser -->
		<property name="parallel" value="${canal_instance_parser_parallel:true}" />
		<property name="parallelThreadSize" value="${canal_instance_parser_parallelThreadSize}" />
		<property name="parallelBufferSize" value="${canal_instance_parser_parallelBufferSize:256}" />
	</bean>

	<bean id="mqConfig" class="com.alibaba.otter.canal.instance.core.CanalMQConfig">
        <property name="topic" value="${canal_mq_topic}" />
		<property name="dynamicTopic" value="${canal_mq_dynamicTopic}" />
        <property name="partition" value="${canal_mq_partition}" />
        <property name="partitionsNum" value="${canal_mq_partitionsNum}" />
        <property name="partitionHash" value="${canal_mq_partitionHash}" />
	</bean>
</beans>
